spring:
  application:
    name: config-zookeeper
  cloud:
    zookeeper:
      enabled: true
      connect-string: localhost:2181
      config:
        root: /cloudoer/${spring.application.name}
        enabale: true
        watcher:
          enable: true
  profiles:
    active: dev