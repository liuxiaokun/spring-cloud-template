server:
  port: 8087
eureka:
  client:
    service-url:
      defaultZone: http://peer1:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
spring:
  application:
    name: service-fred
