eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka/
server:
  port: 8111
spring:
  application:
    name: service-zuul
zuul:
  routes:
    fredapi:
      path: /fredapi/**
      serviceId: service-fred
    ribbonapi:
      path: /ribbonapi/**
      serviceId: service-ribbon
